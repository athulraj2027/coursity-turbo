import { Request, Response } from "express";

const sendOtp = async (req:Request,res:Response)=>{try {
    
} catch (error) {
    
}}

const verifyOtp = async (req:Request,res:Response)=>{try {
    
} catch (error) {
    
}}

export default {sendOtp,verifyOtp}